# 域名部署指南

本文档记录将博客系统部署到服务器并绑定域名的完整流程。

---

## 部署前准备

### 需要准备的资源
- ✅ 已购买的域名（如 `wangyaoyi666.cloud`）
- ✅ 云服务器（CentOS/Ubuntu）
- ✅ 服务器 IP 地址（如 `43.138.32.75`）
- ✅ SSH 登录权限

---

## 第一步：DNS 解析配置

### 1. 进入域名管理控制台

在腾讯云/阿里云等域名注册商控制台，找到「DNS 解析」或「域名解析」。

### 2. 添加 A 记录

添加以下两条解析记录：

| 主机记录 | 记录类型 | 记录值（服务器 IP） | TTL |
|---------|---------|-------------------|-----|
| `@` | A | `43.138.32.75` | 600 |
| `www` | A | `43.138.32.75` | 600 |

> **说明**：
> - `@` 代表根域名（`wangyaoyi666.cloud`）
> - `www` 代表子域名（`www.wangyaoyi666.cloud`）
> - TTL 600 = 10 分钟缓存时间

### 3. 等待 DNS 生效

- 通常 10 分钟到 2 小时内生效
- 使用 `ping wangyaoyi666.cloud` 验证是否解析到正确 IP

---

## 第二步：配置服务器安全组

### 在云服务商控制台配置安全组规则

添加以下入站规则：

| 协议 | 端口 | 来源 | 说明 |
|------|------|------|------|
| TCP | 80 | `0.0.0.0/0` | HTTP |
| TCP | 443 | `0.0.0.0/0` | HTTPS |
| TCP | 22 | `0.0.0.0/0` | SSH（可限制为自己的 IP） |

> **重要**：没有放行 80/443 端口，外网将无法访问网站！

---

## 第三步：上传代码到服务器

### 使用 FTP/SFTP 工具上传

推荐工具：
- **WinSCP**（Windows）
- **FileZilla**（跨平台）
- **Termius**（带 SFTP 功能）

### 上传步骤

1. 连接服务器：
   - 主机：`43.138.32.75`
   - 用户名：`root`
   - 密码/密钥：你的服务器密码

2. 创建目标目录：
   ```bash
   mkdir -p /home/www/BlogSystem
   ```

3. 上传整个项目文件夹到：
   ```
   /home/www/BlogSystem/
   ```

4. 确认上传完成：
   ```bash
   ls -la /home/www/BlogSystem
   # 应该看到 client、server、package.json 等文件
   ```

---

## 第四步：安装依赖并启动应用

### 1. 安装后端依赖并启动

```bash
# 进入后端目录
cd /home/www/BlogSystem/server

# 安装依赖
npm install

# 使用 PM2 启动（推荐，后台运行）
npm install -g pm2
pm2 start server.js --name blog
pm2 save
pm2 startup

# 查看状态
pm2 status
```

> **为什么用 PM2？**
> - 自动守护进程，崩溃自动重启
> - 开机自启动
> - 方便查看日志：`pm2 logs blog`

### 2. 构建前端静态文件

```bash
# 进入前端目录
cd /home/www/BlogSystem/client

# 安装依赖
npm install

# 构建生产版本（生成 dist 目录）
npm run build

# 检查构建结果
ls -la dist/
```

---

## 第五步：安装并配置 Nginx

### 1. 安装 Nginx

```bash
# CentOS
sudo yum install nginx -y

# Ubuntu/Debian
sudo apt install nginx -y

# 启动并设置开机自启
sudo systemctl start nginx
sudo systemctl enable nginx
```

### 2. 创建站点配置文件

```bash
sudo nano /etc/nginx/conf.d/blog.conf
```

### 3. 写入以下配置

```nginx
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name wangyaoyi666.cloud www.wangyaoyi666.cloud;

    # 前端静态文件目录
    root /home/www/BlogSystem/client/dist;
    index index.html;

    # API 请求代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 用户上传的图片等资源
    location /uploads/ {
        alias /home/www/BlogSystem/server/uploads/;
    }

    # 前端路由（SPA 应用）
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 4. 保存并退出

- 按 `Ctrl + O` 保存
- 按 `Enter` 确认
- 按 `Ctrl + X` 退出

### 5. 测试并重载 Nginx

```bash
# 测试配置文件语法
sudo nginx -t

# 重载配置
sudo systemctl reload nginx

# 查看运行状态
sudo systemctl status nginx
```

---

## 第六步：验证部署

### 1. 检查服务状态

```bash
# 检查 Nginx
sudo systemctl status nginx

# 检查 Node.js 应用
pm2 status

# 检查端口监听
netstat -tulnp | grep -E ":(80|443|3001)"
```

### 2. 浏览器访问

打开浏览器，访问：
- `http://wangyaoyi666.cloud`
- `http://www.wangyaoyi666.cloud`

如果看到博客首页，说明部署成功！🎉

---

## 第七步：配置 HTTPS（可选但推荐）

### 1. 安装 Certbot

```bash
# CentOS 7
sudo yum install certbot python-certbot-nginx -y

# CentOS 8/Rocky/AlmaLinux
sudo dnf install certbot python3-certbot-nginx -y

# Ubuntu/Debian
sudo apt install certbot python3-certbot-nginx -y
```

### 2. 自动申请并配置 SSL 证书

```bash
sudo certbot --nginx -d wangyaoyi666.cloud -d www.wangyaoyi666.cloud
```

按提示操作：
1. 输入邮箱（用于证书到期提醒）
2. 同意服务条款
3. 选择是否重定向 HTTP 到 HTTPS（推荐选择 2）

### 3. 测试自动续期

```bash
sudo certbot renew --dry-run
```

> **说明**：Let's Encrypt 证书有效期 90 天，Certbot 会自动续期。

---

## 常见问题排查

### 1. 域名无法访问

**检查 DNS 解析**：
```bash
ping wangyaoyi666.cloud
```

如果 ping 不通或 IP 不对，检查 DNS 配置。

**检查安全组**：
- 确认放行了 80 和 443 端口

### 2. 显示 "Cannot GET /"

**原因**：Nginx 没有正确代理或前端没有构建。

**解决**：
```bash
# 检查 dist 目录是否存在
ls -la /home/www/BlogSystem/client/dist/

# 如果不存在，重新构建
cd /home/www/BlogSystem/client
npm run build

# 重载 Nginx
sudo systemctl reload nginx
```

### 3. API 请求 404

**原因**：后端未运行或端口不对。

**解决**：
```bash
# 检查后端是否运行
pm2 status

# 测试后端端口
curl http://localhost:3001/api/posts

# 如果没响应，重启后端
pm2 restart blog
```

### 4. 502 Bad Gateway

**原因**：Nginx 无法连接到后端（SELinux 阻止）。

**解决**：
```bash
# 检查 SELinux 状态
getenforce

# 如果是 Enforcing，允许 Nginx 网络连接
sudo setsebool -P httpd_can_network_connect 1
```

---

## 日常运维命令

### PM2 管理后端

```bash
pm2 status              # 查看状态
pm2 logs blog           # 查看日志
pm2 restart blog        # 重启应用
pm2 stop blog           # 停止应用
pm2 delete blog         # 删除应用
```

### Nginx 管理

```bash
sudo systemctl status nginx     # 查看状态
sudo systemctl reload nginx     # 重载配置
sudo systemctl restart nginx    # 重启服务
sudo nginx -t                   # 测试配置
```

### 更新代码

```bash
# 1. 上传新代码到服务器（覆盖旧文件）

# 2. 重新构建前端
cd /home/www/BlogSystem/client
npm run build

# 3. 重启后端
pm2 restart blog

# 4. 重载 Nginx（如果配置有改动）
sudo systemctl reload nginx
```

---

## 快捷方式：如果服务器已经部署好

如果你的博客系统**已经通过 IP 地址（如 `http://43.136.32.75`）可以正常访问**，说明前后端、Nginx 都已经配置好了。

这种情况下，要绑定域名只需要：

### 1. 配置 DNS 解析

在域名控制台添加 A 记录（参考"第一步"）：
- `@` → `43.136.32.75`
- `www` → `43.136.32.75`

### 2. 修改 Nginx 配置

编辑配置文件：
```bash
sudo nano /etc/nginx/conf.d/blog.conf
```

找到 `server_name` 这一行，修改为：
```nginx
server_name 43.136.32.75 wangyaoyi666.cloud www.wangyaoyi666.cloud;
```

> **说明**：同时保留 IP 和域名，这样两种方式都可以访问。

### 3. 重载 Nginx

```bash
# 测试配置
sudo nginx -t

# 重载配置
sudo systemctl reload nginx
```

完成！现在访问 `http://wangyaoyi666.cloud` 和访问 IP 的效果完全一样。

**不需要**：
- ❌ 重新上传代码
- ❌ 重启后端服务
- ❌ 重新构建前端
- ❌ 修改应用代码

---

## 总结

### 完整部署流程（从零开始）

1. **DNS 解析**：域名 → 服务器 IP
2. **安全组**：放行 80/443 端口
3. **上传代码**：FTP/SFTP 上传项目文件
4. **后端部署**：npm install + PM2 启动
5. **前端构建**：npm run build 生成 dist
6. **Nginx 配置**：反向代理 API，托管静态文件
7. **HTTPS 配置**：Certbot 自动申请证书
8. **验证上线**：浏览器访问域名

### 快速绑定域名流程（已有服务）

1. **DNS 解析**：域名 → 服务器 IP
2. **修改 Nginx**：在 `server_name` 添加域名
3. **重载 Nginx**：`sudo systemctl reload nginx`

🎉 恭喜！你的博客系统已成功上线！
